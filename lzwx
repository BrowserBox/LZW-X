#!/bin/bash
# lzwx - Compress using LZWX algorithm
# Usage: lzwx [options] <input> <output>

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/arch.py" --mode=compress --algo=lzwx "$@"
